export interface User{
id:number;
age:number;
name:string;
}

export interface Blog{
  postId: number;
  title: string;
  averageReadingTime: number;
  category: string;
}

export interface Group {
    id: number;
    text: string;
  }